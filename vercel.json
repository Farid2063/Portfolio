{
  "buildCommand": "cd portfolio && npm install && npx prisma generate --schema=../prisma/schema.prisma && npm run build",
  "outputDirectory": "portfolio/.next",
  "installCommand": "cd portfolio && npm install",
  "framework": "nextjs"
}

